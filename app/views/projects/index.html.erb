<% @projects.each do |project| %>
  <tr data-controller="project"
      data-project-url-value="<%= project_path(project.id) %>"
  >
    <%= render 'project', project: project, harvest_company: nil, moneybird_sales_invoice: nil %>
  </tr>
<% end %>
